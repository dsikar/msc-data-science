%ans =

%    25    91
%    15   115
%    40   288
%    34   294
%    44   299

% fnRotateImages('../images/sandbox_input/25','/images/sandbox_extra/25_aug', 100, -4)

% rotation
% for i=1:20
%     disp(i)
% end

% brightness

%fnRotateImages('../images/sandbox_input/25','/images/sandbox_aug/25', 100, -4)

%degs = [-10 -5 5 10];
%x = size(degs);
% Rotate some images
%for i=1:x(2)
%   disp(degs(1,i))
%   fnRotateImages('../images/sandbox_input/25','/images/sandbox_aug/25', 100, degs(1,i))
%end

% Get stills - done manually

% Rotate some images
dirpath = '../images/sandbox_input/26';
folders = dir(dirpath);
% initialise counts array
p = [0 0];
% initialise counts struct
s(1).ID = '';
s(1).count = 0;
offset = 3; % skip hidden sub-directories on windows e.g. "." and ".."
for i=offset:length(folders)

% Blur some rotated images

% Blur some images



% Set a threshold

% Sample augmented

% Crop images